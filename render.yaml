services:
  - type: web
    name: MoodFlix
    env: python
    buildCommand: "pip install -r requirements.txt && python src/preprocessing.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
